conn ikev2-{{ newtroy_server_cert_common_name }}
    fragmentation=yes
    rekey=no
    dpdaction=clear
    keyexchange=ikev2
    compress=no
    dpddelay=35s

{% if Win10_Enabled is defined and Win10_Enabled == "Y" %}
    ike={{ ciphers.compat.ike }}
    esp={{ ciphers.compat.esp }}
{% else %}
    ike={{ ciphers.defaults.ike }}
    esp={{ ciphers.defaults.esp }}
{% endif %}

    right={{ newtroy_server_cert_common_name }}
    rightid={{ newtroy_server_cert_common_name }}
    rightsubnet=0.0.0.0/0
    rightauth=pubkey

    leftsourceip=%config
    leftauth=pubkey
    leftid={{ item }}
    leftcert={{ item }}.crt
    leftfirewall=yes
    left=%defaultroute

    auto=add
